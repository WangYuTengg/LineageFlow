## To Run Locally:
1. Make sure to have npm installed.
2. Navigate to the client directory:
    ```bash
    cd client
    ```
3. Install dependencies:
    ```bash
    pnpm install
    ```
4. Run the client:
    ```bash
    pnpm run dev
    ```